import sqlite from "../../../backend/storage/database/sqlite/sqlite";

class EventsSync {
  async initEventsSyncDatabase() {
    try {
      const DB = sqlite.connectDB();
      await DB.
    } catch (err) {}
  }
}
